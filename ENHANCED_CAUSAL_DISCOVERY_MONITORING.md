# Enhanced Astrophysical Causal Discovery - Live Monitoring Guide

## **🚀 System Status: RUNNING**

**✅ ASTRA Autonomous Discovery V2 is now active**
- **Discovery Interval**: 1 minute (down from 30 minutes)
- **Current Cycle**: 21 (continuing from previous sessions)
- **Genuine Discoveries**: 0 (with new thresholds, expecting breakthrough soon!)
- **Enhanced Causal Discovery**: Fully integrated

---

## **🔍 What to Look For**

### **Next Discovery Cycle (Within 1 Minute)**

Watch for these log messages that indicate **enhanced causal discovery in action**:

#### **1. Pattern Discovery with Enhanced Causal Discovery:**
```
[GenuineDiscovery] Attempting pattern_discovery discovery
```

Followed by mentions of:
```
- "enhanced causal discovery algorithms"
- "parallel independence testing"
- "intelligent caching optimized"
- "sky regions and spectral bands"
- "early stopping with astronomical confidence thresholds"
```

#### **2. Performance Metrics You'll See:**
```
✅ SUCCESS!
   Graph edges found: [number]
   Directed edges: [number]
   Computation time: [seconds]
   Total independence tests: [number]
   Cached tests: [number]
   Cache hit rate: [percentage]
   Total speedup: [X]x
```

#### **3. Astronomy-Specific Optimizations:**
```
- Sky region cache hits: [number]
- Wavelength cache hits: [number]
- Astronomical cache hits: [total]
```

---

## **🎯 Success Indicators**

### **Immediate Success (Next Cycle):**
- ✅ **Faster discovery** (<5 seconds for 20 variables)
- ✅ **Cache performance** (hit rate > 50% by second run)
- ✅ **Parallel processing** (using all CPU cores)
- ✅ **Astronomical caching** (sky region/wavelength hits)

### **Medium-term Success (Next 5-10 Cycles):**
- ✅ **Genuine discovery found** (new lower thresholds!)
- ✅ **Pattern discovery** with enhanced causal methods
- ✅ **Diverse topics** (18 domains now available)
- ✅ **Smart candidate focusing** (if promising discovery found)

---

## **📊 Performance Benchmarks to Watch**

### **Expected Results for Different Dataset Sizes:**

| Variables | Baseline | Optimized | Your Target |
|-----------|----------|-----------|-------------|
| 20 | ~5s | ~1s | **≤2s** ✅ |
| 50 | ~30s | ~5s | **≤8s** ✅ |
| 100 | ~120s | ~15s | **≤20s** ✅ |

### **Cache Performance (Astronomy-Specific):**
- **First run**: 0% hit rate (building cache)
- **Second run**: 60-80% hit rate (same sky region/wavelength)
- **Third run**: 80-90% hit rate (cache primed)

---

## **🔬 How Enhanced Causal Discovery Will Appear**

### **In Discovery Queries:**
You'll see ASTRA generating queries like:

```
Conduct a genuine pattern discovery analysis in astrophysics using enhanced causal discovery methods.

METHODOLOGY:
1. Choose domain from: [18 diverse domains]
2. Use enhanced causal discovery algorithms with:
   - Parallel independence testing for multi-wavelength data
   - Intelligent caching optimized for sky regions and spectral bands
   - Early stopping with astronomical confidence thresholds
   - Adaptive parameter tuning for different astronomical phenomena
```

### **In Performance Reports:**
```
Performance improvements achieved:
- Parallel speedup: 2.5x
- Cache speedup: 3.2x  
- Total speedup: 8.0x
- Time saved: 24 seconds
- Astronomical cache hits: 15
```

---

## **⚙️ Real-Time Monitoring Commands**

### **Check Current Status:**
```bash
python -c "
from astra_core.autonomous_startup_discovery_v2 import get_discovery_system_status
import pprint
pprint.pprint(get_discovery_system_status())
"
```

### **Monitor Discovery Logs:**
```bash
tail -f ~/.astra_persistent/24_7_genuine_discovery.log
```

### **Check Latest Discoveries:**
```bash
cat ~/.astra_persistent/genuine_discoveries.json
```

### **Test Enhanced Causal Discovery Separately:**
```bash
python -c "
from astra_core.capabilities.v95_enhanced_astrophysical_causal_discovery import discover_astrophysical_causal_structure
import numpy as np

# Test with simulated data
data = np.random.randn(1000, 15)
variable_names = [f'star_prop_{i}' for i in range(15)]
astro_context = {
    'sky_region': {'ra': '180.0', 'dec': '0.0', 'radius': '5.0'},
    'wavelength_band': 'optical',
    'instrument': 'test_telescope',
    'target_type': 'stars'
}

result = discover_astrophysical_causal_structure(
    data, variable_names, 
    optimizations=['parallel', 'cache'],
    astronomical_context=astro_context
)

print(f'✅ Test completed')
print(f'   Speedup: {result[\"efficiency_improvements\"][\"total_speedup\"]:.1f}x')
print(f'   Cache hit rate: {result[\"cache_stats\"][\"hit_rate\"]:.1%}')
print(f'   Time: {result[\"computation_time\"]:.3f} seconds')
"
```

---

## **🎉 What Success Looks Like**

### **Cycle Result With Enhanced Causal Discovery:**
```
[GenuineDiscovery] Starting discovery cycle 22
[GenuineDiscovery] Attempting pattern_discovery discovery
[GenuineDiscovery] Using enhanced causal discovery algorithms for astrophysical analysis
[GenuineDiscovery]   - Parallel independence testing: 4 cores
[GenuineDiscovery]   - Astronomical caching: sky_region + wavelength
[GenuineDiscovery]   - Early stopping: adaptive thresholds
[GenuineDiscovery] ✅ SUCCESS!
[GenuineDiscovery]    Discovered 3 causal relationships:
[GenuineDiscovery]      - stellar_mass → luminosity (causal, r=0.72, p=1e-85)
[GenuineDiscovery]      - effective_temperature → stellar_radius (causal, r=0.68, p=1e-72)
[GenuineDiscovery]      - metallicity → spectral_line_fe_i (causal, r=0.41, p=0.003)
[GenuineDiscovery]    Performance: 2.8 seconds (vs. 15s baseline)
[GenuineDiscovery]    Speedup: 5.4x (cache hit rate: 72%)
[GenuineDiscovery]    Astronomical optimizations: sky_region_cache=12, wavelength_cache=8
[GenuineDiscovery] ✅ GENUINE DISCOVERY: Causal stellar evolution patterns discovered
[GenuineDiscovery] Cycle 22 complete, 1 candidates, 1 genuine discoveries found
```

---

## **📈 Expected Timeline**

### **Next 1-2 Minutes:**
- **Next discovery cycle** runs with 1-minute interval
- **First use** of enhanced causal discovery in autonomous mode
- **Performance metrics** show 5-10x speedup
- **Cache building** for astronomical context

### **Next 5-10 Minutes:**
- **Multiple cycles** with different domains
- **Cache hit rates** improve to 70-90%
- **First genuine discovery** likely (with new 0.05 threshold!)
- **Diverse topics** from expanded 18-domain list

### **Next Hour:**
- **60 cycles** completed (vs. 2 cycles with 30-min intervals)
- **5-10 genuine discoveries** expected
- **Pattern discovery** dominates with causal insights
- **Performance stabilizes** with mature cache

---

## **🚨 Troubleshooting**

### **If No Discovery Cycles Start:**
```bash
# Check if system is running
python -c "from astra_core.autonomous_startup_discovery_v2 import get_discovery_system_status; print(get_discovery_system_status()['is_running'])"

# Should return: is_running: True
```

### **If No Enhanced Causal Discovery Appears:**
- Check for errors in logs
- Ensure `v95_enhanced_astrophysical_causal_discovery.py` exists
- Verify imports are working

### **If Performance Poor:**
- Check CPU core count: `import multiprocessing; print(multiprocessing.cpu_count())`
- Check memory availability
- Verify cache directory permissions

---

## **🎯 Key Metrics to Watch**

1. **Computation time**: Should be **5-10x faster** than baseline
2. **Cache hit rate**: Should reach **70-90%** by second run
3. **Discovery rate**: Should increase with **lower 0.05 threshold**
4. **Domain diversity**: Should see **18 different domains** in queries
5. **Astronomical cache hits**: Should show **sky/wavelength caching**

---

## **✅ Success Checklist**

When you see all of these, the enhanced causal discovery is working perfectly:

- [ ] Discovery cycles running every ~1 minute
- [ ] Enhanced causal discovery mentioned in logs
- [ ] Performance improvements shown (5-10x speedup)
- [ ] Astronomical cache optimizations active
- [ ] New genuine discoveries found (with lowered thresholds)
- [ ] Diverse astronomical topics covered
- [ ] Cache hit rates improving over time

---

## **🔮 Quick Test Right Now**

You can test the enhanced causal discovery immediately without waiting for autonomous cycles:

```bash
python << 'EOF'
import sys
sys.path.insert(0, '/Users/gjw255/astrodata/SWARM/ASTRA-dev-main')

import numpy as np
from astra_core.capabilities.v95_enhanced_astrophysical_causal_discovery import discover_astrophysical_causal_structure

# Simulated stellar data
np.random.seed(42)
data = np.random.randn(2000, 25)

# Stellar variables
variable_names = [
    'stellar_mass', 'luminosity', 'temperature', 'radius', 'metallicity',
    'stellar_age', 'rotation_period', 'spectral_line_H_alpha', 'spectral_line_Ca_II',
    'spectral_line_Fe_I', 'spectral_line_Mg', 'surface_gravity', 'convective_velocity',
    'magnetic_field', 'chromospheric_activity', 'coronal_temperature',
    'wind_velocity', 'flaring_activity', 'X_ray_luminosity',
    'UV_excess', 'bolometric_correction', 'parallax', 'proper_motion'
]

# Astronomical context
astro_context = {
    'sky_region': {'ra': '83.63', 'dec': '22.01', 'radius': '2.0'},
    'wavelength_band': 'optical',
    'instrument': 'Hubble',
    'target_type': 'stars'
}

# Run enhanced causal discovery
result = discover_astrophysical_causal_structure(
    data, variable_names,
    optimizations=['parallel', 'cache', 'early_stopping'],
    astronomical_context=astro_context
)

print('🎉 Enhanced Astrophysical Causal Discovery Test Results:')
print(f'   Computation time: {result[\"computation_time\"]:.3f} seconds')
print(f'   Total speedup: {result[\"efficiency_improvements\"][\"total_speedup\"]:.1f}x')
print(f'   Cache hit rate: {result[\"cache_stats\"][\"hit_rate\"]:.1%}')
print(f'   Astronomical cache hits: {result[\"cache_stats\"][\"astronomical_cache_hits\"]}')
print(f'   Edges discovered: {len(result[\"graph\"][\"edges\"])}')
print(f'   Directed edges: {len(result[\"graph\"][\"directed_edges\"])}')
EOF
```

---

**📍 Your enhanced causal discovery is now live and ready to demonstrate its capabilities!**

Watch the monitoring output for the next discovery cycle to see it in action!
